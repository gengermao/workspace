#include<stdio.h>
main(){
	int i, c, white_num, other_num;
	int digitnum[10];

	for(i = 0; i < 10; i++){
		digitnum[i] = 0;
	}

	while((c = getchar()) != EOF){
		if(' ' == c || '\t' = c || '\n' = c)
			++white_num;
		else if(c)

	}

}
